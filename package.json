{
  "name": "khadamat-platform",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "imports": {
    "axios": "./scripts/ops-axios.js"
  },
  "workspaces": [
    "client",
    "server",
    "backend"
  ],
  "devDependencies": {
    "axios": "^1.6.0"
  },
  "scripts": {
    "dev": "node ./scripts/dev.mjs",
    "dev:demo": "npm run dev --workspace server",
    "dev:frontend": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "dev:backend": "npm run dev --workspace backend",
    "build": "npm run build --workspace client && npm run build --workspace backend",
    "build:client": "npm run build --workspace client",
    "build:server": "npm run build --workspace server",
    "start": "npm run start --workspace backend",
    "start:prod": "npm run start --workspace backend",
    "start:demo": "npm run start --workspace server",
    "start:backend": "npm run start --workspace backend",
    "smoke:all": "npm run smoke:all:strict --workspace backend",
    "ci:check": "npm run ci:check --workspace backend",
    "seed": "npm run seed --workspace backend",
    "test": "npm run test --workspace client",
    "lint": "npm run lint --workspace client",
    "check": "npm run check --workspace server",
    "check:services": "npm run check:services --workspace client",
    "check:services:online": "npm run check:services:online --workspace client",
    "db:push": "npm run db:push --workspace server",
    "backend:ops:verify": "npm run ops:verify --workspace backend",
    "ops:verify": "node scripts/ops-verify.js",
    "online:check": "node backend/scripts/env/load.js backend/.env.online.staging backend/.env.tokens.staging -- npm run ops:verify --workspace backend",
    "online:smoke": "node backend/scripts/env/load.js backend/.env.online.staging backend/.env.tokens.staging -- npm run online:all --workspace backend"
  }
}
