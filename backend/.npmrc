# Registre principal
registry=https://registry.npmjs.org/
strict-ssl=true

# Use public mirror for DefinitelyTyped packages when npmjs.org is restricted
@types:registry=https://registry.npmmirror.com/

# Forcer @prisma/* sur un miroir compatible
@prisma:registry=https://registry.npmmirror.com/

; Autoriser override par ENV
; npm_config_@prisma:registry a priorité si défini côté CI

# Évite les re-downloads
prefer-offline=true
fund=false
audit=false

# Alternative mirror (à activer SEULEMENT si npmjs.org est bloqué)
; registry=https://registry.npmmirror.com/
; @prisma:registry=https://registry.npmjs.org/
